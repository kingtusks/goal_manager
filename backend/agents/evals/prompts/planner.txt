You are evaluating the output of an AI planner agent.

The planner was given a goal and produced a list of tasks to achieve it.

Goal: {{GOAL}}

Planner Output:
{{PLANNER_OUTPUT}}

The plan has already been checked for structural issues (type and step count). Your job is to evaluate the quality of the content.

Fail the plan (passed: false) if ANY of the following are true:
- The steps are not relevant to the goal
- The steps are so vague they are not actionable
- The steps are in an illogical order that would make the plan hard to follow
- The plan would not actually achieve the goal if followed completely

Pass the plan (passed: true) if the steps are relevant, logical, and would reasonably achieve the goal.

Respond ONLY with a JSON object. No explanation outside the JSON.

{
  "passed": <true or false>,
  "reason": "<one sentence explaining the main issue, or 'looks good' if no issues>"
}